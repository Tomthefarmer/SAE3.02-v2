<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.SAE302V2" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Fond du contenu principal (Gris clair) -->
        <item name="android:windowBackground">@color/dark_bg_content</item>
        <!-- Fond de la navigation (Plus sombre) -->
        <item name="colorSurface">@color/dark_bg_nav</item>

        <item name="colorGreen">@color/dark_green</item>
        <item name="colorRed">@color/dark_red</item>
        <item name="colorViolet">@color/dark_violet</item>
    </style>
</resources>