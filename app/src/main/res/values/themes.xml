<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.SAE302V2" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SAE302V2" parent="Base.Theme.SAE302V2" />

    <!-- Pour la calculatrice -->
    <!-- Style pour arrondir les boutons -->
    <style name="ShapeAppearance.App.Circle" parent="">
        <item name="cornerSize">50%</item>
    </style>
    <!-- Style des boutons de la calculatrice -->
    <style name="CalcButtonStyle" parent="Widget.Material3.Button">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">80dp</item>
        <item name="android:layout_columnWeight">1</item>
        <item name="android:layout_margin">4dp</item>
        <item name="shapeAppearance">@style/ShapeAppearance.App.Circle</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>